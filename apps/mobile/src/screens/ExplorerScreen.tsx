import { Text, View } from "react-native";

/**
 * Pantalla principal del módulo Explorer.
 * Actúa como punto de entrada visual para la exploración de contenido dentro de la aplicación.
 *
 * @returns {JSX.Element} Vista base del Explorer.
 */
export default function ExplorerScreen() {
  return (
    <View>
      <Text>Explorer Screen!</Text>
    </View>
  );
}
